

## Plan: Update Hero Text

Two small text changes in `src/pages/HomePage.tsx`:

1. **Line 405**: Change `{BRAND.name}` → `Saturn Terminal` (hardcoded, since "Terminal" isn't part of branding config)
2. **Line 413**: Change `The fastest AI-powered trading terminal on Solana` → `The fastest trading terminal on Solana`

Also update `src/config/branding.ts` line 15 tagline to match: `"The fastest trading terminal on Solana"`

